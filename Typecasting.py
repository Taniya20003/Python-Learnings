# When there is a need to change the type of data for operations etc.
# There are functions like int(),tuple(),float(), str(), dict() etc.
# There are two types of typecasting:
# Explicit: That is done manually
# Implicit: That is done by Python interpreter automatically.

a="1"
b="2"

print(a+b)
print(int(a)+int(b))